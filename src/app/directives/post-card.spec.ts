import { ProductCardDirective } from './post-card';

describe('PostCard', () => {
  it('should create an instance', () => {
    const directive = new ProductCardDirective();
    expect(directive).toBeTruthy();
  });
});
